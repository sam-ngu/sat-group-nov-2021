// CRUD

/**
 * Create a new role in db.
 * @param {string} name
 * @param {string} salary
 * @param {number} departmentId
 */
function createRole(name, salary, departmentId) {
    //
}

/**
 * Getting all the roles from db
 */
function getRoles() {}

// [for the future]
// function updateRole(id, payload) {

// }

module.exports = {
    createRole,
    getRoles,
};
